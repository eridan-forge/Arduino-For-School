/*
Уровень 1: Изменённые временные параметры
- Зелёный горит 3 секунды вместо 5
- Жёлтый мигает 5 раз вместо 3
- Добавлена пауза 1 секунда между фазами
*/

void setup() {
  pinMode(11, OUTPUT);  // 🔴 Красный
  pinMode(12, OUTPUT);  // 🟡 Жёлтый
  pinMode(13, OUTPUT);  // 🟢 Зелёный
  Serial.begin(9600);
}

void loop() {
  // 🔴 Фаза 1: Красный 5 секунд
  Serial.println("🔴 КРАСНЫЙ: СТОП (5 секунд)");
  digitalWrite(11, HIGH);
  digitalWrite(12, LOW);
  digitalWrite(13, LOW);
  delay(5000);
  
  // ⏸️ Пауза между фазами (1 секунда всё выключено)
  Serial.println("⏸️ ПАУЗА (1 секунда)");
  digitalWrite(11, LOW);
  digitalWrite(12, LOW);
  digitalWrite(13, LOW);
  delay(1000);
  
  // 🟢 Фаза 2: Зелёный 3 секунды (изменено!)
  Serial.println("🟢 ЗЕЛЁНЫЙ: ИДТИ (3 секунды)");
  digitalWrite(11, LOW);
  digitalWrite(12, LOW);
  digitalWrite(13, HIGH);
  delay(3000);  // Было 5000, стало 3000
  
  // ⏸️ Пауза
  Serial.println("⏸️ ПАУЗА (1 секунда)");
  digitalWrite(11, LOW);
  digitalWrite(12, LOW);
  digitalWrite(13, LOW);
  delay(1000);
  
  // 🟡 Фаза 3: Жёлтый мигает 5 раз (изменено!)
  Serial.println("🟡 ЖЁЛТЫЙ: ВНИМАНИЕ (5 миганий)");
  for (int i = 1; i <= 5; i++) {  // Было i <= 3, стало i <= 5
    Serial.print("  Мигание ");
    Serial.println(i);
    
    digitalWrite(12, HIGH);
    delay(500);
    digitalWrite(12, LOW);
    delay(500);
  }
  
  Serial.println("=== ЦИКЛ ЗАВЕРШЁН ===\n");
}
