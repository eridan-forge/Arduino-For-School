# 📘 УРОК 3: КНОПКА И СВЕТОДИОД

## 🎯 Цели урока
1. Научиться подключать кнопку к Arduino
2. Понимать разницу между INPUT и INPUT_PULLUP
3. Познакомиться с понятием "дребезг контактов"
4. Создать первое интерактивное устройство

## 📦 Необходимые компоненты
- Arduino Nano/Uno
- Тактовая кнопка
- Светодиод
- Резистор 220 Ом
- Провода

## 🔌 Схема подключения
Arduino → Компоненты:
D2 → Кнопка (верхний левый штырь) → GND
D13 → Светодиод(+) → Резистор 220Ω → GND

## 📁 Структура уроков
1. **01-basic-button/** - Простейшая кнопка (без защиты от дребезга)

   [Пример в симуляторе вовки](https://wokwi.com/projects/454143089294462977)

3. **02-button-debounce/** - Кнопка с защитой от дребезга (рекомендуется)

   [Пример в симуляторе вовки](https://wokwi.com/projects/453946578656790529)

5. **03-button-hold/** - Кнопка с удержанием

   [Пример в симуляторе вовки](https://wokwi.com/projects/454145200635960321)

8. **04-button-counter/** - Счётчик нажатий

   [Пример в симуляторе вовки](https://wokwi.com/projects/454145894106486785)

## 🎮 Задания для самостоятельной работы
- Измени пин светодиода на D8
- Добавь второй светодиод
- Создай "игру на реакцию"

## 🚨 Типичные ошибки
1. Кнопка подключена неправильно (не те ножки)
2. Нет INPUT_PULLUP в коде
3. Забыт резистор для светодиода

# 📝 Инструкция для урока 3

## Шаг 1: Подготовка
1. Откройте Wokwi/Wowki
2. Добавьте Arduino Nano
3. Добавьте кнопку и светодиод

## Шаг 2: Подключение
1. Подключите кнопку:
   - D2 → левый верхний штырь кнопки
   - Правый верхний штырь кнопки → GND

2. Подключите светодиод:
   - D13 → длинная нога светодиода (+)
   - Короткая нога светодиода (-) → резистор 220Ω → GND

## Шаг 3: Код
1. Начните с `01-basic-button/`
2. Запустите и увидите проблему дребезга
3. Перейдите к `02-button-debounce/` для решения

## Шаг 4: Эксперименты
1. Измените пин светодиода с D13 на D8
2. Добавьте второй светодиод
3. Создайте свою логику работы кнопки

## 🔧 Если не работает:
1. Проверьте подключение кнопки
2. Убедитесь что используете INPUT_PULLUP
3. Проверьте полярность светодиода

# 🚨 Типичные ошибки

## 1. Кнопка всегда "нажата"
**Причина:** Неправильное подключение или нет INPUT_PULLUP
**Решение:** 
- Проверьте что кнопка подключена к D2 и GND
- Используйте `pinMode(2, INPUT_PULLUP)`

## 2. Светодиод не загорается
**Причина:** Неправильная полярность или нет резистора
**Решение:**
- Длинная нога (+) к Arduino
- Короткая нога (-) к резистору → GND
- Обязательно резистор 220Ω

## 3. Много срабатываний от одного нажатия
**Причина:** Дребезг контактов
**Решение:** Используйте код из папки `02-button-debounce/`

## 4. Кнопка не реагирует
**Причина:** Не те ножки кнопки
**Решение:** Попробуйте другие пары ножек

# ❓ Тестовые вопросы

## Вопрос 1
Почему мы используем `INPUT_PULLUP` для кнопки?

Ответ:
Мы используем INPUT_PULLUP, чтобы избежать "плавающего состояния" пина. Когда кнопка не нажата, пин D2 должен иметь чёткое значение (HIGH или LOW), а не случайные значения.

Простыми словами:

Без INPUT_PULLUP: Когда кнопка не нажата, пин "висит в воздухе" → Arduino видит случайные 0 и 1 → ложные срабатывания.

С INPUT_PULLUP: Arduino включает внутренний резистор, который "подтягивает" пин к 5V → всегда видит HIGH (1) когда кнопка не нажата.

При нажатии: Кнопка соединяет пин с GND → Arduino видит LOW (0).

Аналогия: Как качели привязанные верёвкой кверху (HIGH). Нажатие кнопки = разрезали верёвку, качели упали (LOW).

## Вопрос 2
Что такое "дребезг контактов" и как с ним бороться?

Ответ:
Дребезг контактов — это механическая проблема, когда при нажатии кнопки её контакты соприкасаются не сразу, а несколько раз за миллисекунды (как мячик, который подпрыгивает при ударе об пол).

Что происходит:

Ты нажимаешь кнопку 1 раз

Контакты соприкасаются 10-20 раз за 50 мс

Arduino очень быстрый, считает каждое касание как отдельное нажатие

Вместо одного срабатывания получаем 10-20

Как бороться:

Программная задержка: После обнаружения нажатия ждём 50-300 мс

Флаг состояния: Запоминаем что уже обработали это нажатие

Двойная проверка: Через 50 мс проверяем, всё ещё нажата ли кнопка

if (digitalRead(button) == LOW && !обработано) {
  обработано = true;
  delay(300);  // Ждём пока "устаканится"
  // Действие
}

## Вопрос 3
Зачем нужна переменная `buttonPressed` в коде с антидребезгом?

Переменная buttonPressed — это флаг-помощник, который запоминает: "Мы уже обработали это нажатие, не трогай пока!"

Без buttonPressed:

Кнопка нажата → делаем действие

Но дребезг продолжается 300 мс

Arduino снова видит "нажатие" (это дребезг!)

Делает действие ещё раз... и ещё... и ещё...

С buttonPressed:

Первое нажатие: buttonPressed = false → выполняем действие → buttonPressed = true

Дребезг: Arduino проверяет → buttonPressed = true → игнорируем!

Отпустили кнопку: buttonPressed = false (готовы к новому нажатию)

Простыми словами: Как охранник на дискотеке — один билет на одного человека. buttonPressed = "билет уже использован".

## Вопрос 4
Почему светодиоду нужен резистор?

Ответ:
Резистор нужен, чтобы ограничить ток через светодиод и не сжечь его.

Физика:

Светодиод хочет пропускать МНОГО тока (как открытый кран)

Arduino даёт 5V и может дать много тока

Без резистора светодиод сгорит (перегреется)

Цифры:

Типичный светодиод: 20 мА (0.02 А), 2V

Arduino пин: 5V, до 40 мА

Резистор 220Ω создаёт "узкое место": ток = (5V - 2V) / 220Ω ≈ 14 мА

Светодиод работает, не горит

Аналогия:

Без резистора = открыть пожарный гидрант на полную в стакан → стакан разорвёт

С резистором = поставить регулятор → течёт столько, сколько нужно

## Вопрос 5
Как сделать чтобы кнопка включала светодиод только при удержании?

Ответ:
Нужна простая логика: "горит, пока нажато; не горит, когда отпущено".

void loop() {
  if (digitalRead(button) == LOW) {
    digitalWrite(led, HIGH);  // ВКЛ, пока нажато
  } else {
    digitalWrite(led, LOW);   // ВЫКЛ, когда отпущено
  }
}

Как это работает:

Нажали кнопку → digitalRead(button) == LOW → digitalWrite(led, HIGH)

Держите кнопку → условие всё ещё истинно → светодиод горит

Отпустили кнопку → digitalRead(button) == HIGH → digitalWrite(led, LOW)

Где используется:

Дверной звонок (звенит пока держишь кнопку)

Фонарик с кнопкой

Паяльник с кнопкой безопасности

Любое устройство где "работает только при удержании"

Отличие от обычной кнопки:

Обычная: нажал → переключилось состояние (ВКЛ/ВЫКЛ)

Удержание: нажал → работает, отпустил → не работает (как выключатель света)





INPUT_PULLUP — чтобы не было случайных срабатываний

Дребезг — кнопка "дрожит" при нажатии, решаем задержкой

buttonPressed — флаг "уже обработали, не трогать"

Резистор — ограничитель тока, чтобы не сжечь светодиод

Удержание — if(нажата){ВКЛ}else{ВЫКЛ}
